namespace RPGMaker.Codebase.Runtime.Common.Enum
{
    /// <summary>
    /// 入力種別
    /// </summary>
    public enum HandleType
    {
        Left, //左キー
        Right, //右キー
        Up, //上キー
        Down, //下キー
        Decide, //決定キー
        Back, //戻るキー
        LeftKeyDown, //左キー押下
        RightKeyDown, //右キー押下
        UpKeyDown, //上キー押下
        DownKeyDown, //下キー押下
        LeftClick, //左クリック
        RightClick, //右クリック
        LeftShiftDown, //左シフト押下
        LeftShiftUp, //左シフトを離す
        PageLeft, //前ページ
        PageRight //次ページ
    }
}
